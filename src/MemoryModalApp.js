import React from 'react';
import './MemoryModal.css';  // App 전체에 적용되는 CSS
import MemoryModal from './MemoryModal';  // MemoryModal 컴포넌트 불러오기

function App() {
    return (
        <div className="App">
            <MemoryModal />
        </div>
    );
}

export default App;
